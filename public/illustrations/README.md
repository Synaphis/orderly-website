# Orderly Minimal SVG Illustrations V2

These are more illustrative, more spacious mini-scenes with less overlap.

Place this folder at:

public/illustrations/

The SVGs use currentColor, so set the parent color:

style={{ color: "#fff" }}

Example:

<img src="/illustrations/hero-orderly.svg" alt="" aria-hidden="true" />
